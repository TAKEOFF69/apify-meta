{
  "name": "@twenty50/apify-social-scraper",
  "version": "2.0.0",
  "type": "module",
  "description": "Apify actor: scrape competitor IG + FB via API/HTTP (no browser)",
  "scripts": {
    "start": "node build/main.js",
    "build": "tsc",
    "dev": "npx tsx src/main.ts"
  },
  "dependencies": {
    "apify": "^3.2.0",
    "undici": "^7.22.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20"
  }
}
